ext {
    // Android libraries
    app_compat_version = '1.1.0-alpha04'
    core_version = '1.1.0-alpha05'
    constraint_layout_version = '2.0.0-alpha5'
    material_design_version = '1.1.0-alpha05'

    // Koin
    koin_core_version = '1.0.2'

    // Leak Canary
    leak_canary_version = '1.6.3'

    // Glide
    glide_version = '4.9.0'

    // Unit Testing
    junit_version = '4.12'

    // Instrumentation Testing
    runner_version = '1.1.0-alpha4'
    espresso_core_version = '3.1.0-alpha4'
}